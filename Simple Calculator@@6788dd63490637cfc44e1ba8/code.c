#include <stdio.h>

int main() {
    int a,b;
    char ch;
    scanf("%d %d %c",&a ,&b ,&ch);
    if (char ch == +) {
        printf(a + b);
    } else if (char ch == -) {
        printf(a - b);
    } else if (char ch == * ) {
        printf(a * b);
    } else (char ch == /) {
        printf(a / b);
    }
    return 0;
}