#include <stdio.h>

int main() {
    int a,b,c;
    scanf("%d",&a);
    scanff("%d",&b);
    scanf("%d",&c);
    if ((a + b) > c)) {
        printf("Valid");
    } else {
        printf("Invalid");
    }
    return 0;
}